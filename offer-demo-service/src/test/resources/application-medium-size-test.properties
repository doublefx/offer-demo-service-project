#database
offer.db.host=${embedded.mariadb.host}
offer.db.port=${embedded.mariadb.port}
offer.db.schema=${embedded.mariadb.schema}
spring.datasource.username=${embedded.mariadb.user}
spring.datasource.password=${embedded.mariadb.password}
spring.jpa.hibernate.ddl-auto=create-drop